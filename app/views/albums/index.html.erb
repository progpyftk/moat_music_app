<div class="container d-grid gap-2">
  <div class="">
    <%= link_to "ADD ALBUM", new_album_path, class:"btn btn-primary btn-lg"%>
  </div>
  <div class="p-2 bg-light border">
    <table class="table table-bordered">
      <thead>
        <tr>
          <th>Album Name</th>
          <th>Artist</th>
          <th>Year</th>
          <th>Create by</th>
          <th>Delete</th>
        </tr>
      </thead>
      <tbody>
        <% @albums.each do |album| %>
          <tr>
            <td><%= album.name %></td>
            <td><%= album.artist %></td>
            <td><%= album.year %></td>
            <td><%= album.user.username %></td>
            <td>
              <% if current_user == album.user %>
                <%= link_to "Delete", album, method: :delete, class:"btn btn-danger" ,data: { confirm: "You sure?" } %>
              <% else %>
                Can't Delete
              <% end %>
            </td>
          <% end %>
        </tr>
      </tbody>
    </table>
  </div>
</div>
